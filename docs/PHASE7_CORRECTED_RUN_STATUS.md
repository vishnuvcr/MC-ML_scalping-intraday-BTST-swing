# Corrected Run Status

The empirical run is deliberately not being promoted yet. The first simulator exposed a material accounting error, which has been quarantined. The corrected specification is now frozen before another numerical run.

The next run must satisfy: no impossible compounding, explicit cash/equity accounting, conservative same-bar stop handling, realistic costs, no overlap, and train/test isolation.
